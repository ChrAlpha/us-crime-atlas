import type { AtlasState,CrimeGroup,SelectedPlace } from '../types';
const VALID_GROUPS:CrimeGroup[]=['violent','property','vehicle','weapons','other'];const VALID_RADII=[500,1000,2000] as const;const VALID_WINDOWS=[14,30,60,90] as const;
export const DEFAULT_PLACE:SelectedPlace={label:'Times Square',detail:'Manhattan, New York, NY',coordinates:[-73.9855,40.758]};
export const DEFAULT_ATLAS_STATE:AtlasState={place:DEFAULT_PLACE,radiusMeters:1000,windowDays:30,activeGroups:[...VALID_GROUPS]};
const finiteNumber=(value:string|null)=>{if(value===null||value.trim()==='')return null;const parsed=Number(value);return Number.isFinite(parsed)?parsed:null;};
export function parseUrlState(search:string):AtlasState{const params=new URLSearchParams(search);const longitude=finiteNumber(params.get('lng'));const latitude=finiteNumber(params.get('lat'));const radius=finiteNumber(params.get('radius'));const days=finiteNumber(params.get('days'));const groups=params.get('groups')?.split(',').filter((group):group is CrimeGroup=>VALID_GROUPS.includes(group as CrimeGroup));const coordinates=longitude!==null&&latitude!==null&&Math.abs(longitude)<=180&&Math.abs(latitude)<=90?([longitude,latitude] as const):DEFAULT_PLACE.coordinates;return{place:{label:params.get('place')?.slice(0,120)||DEFAULT_PLACE.label,detail:params.get('detail')?.slice(0,180)||DEFAULT_PLACE.detail,coordinates},radiusMeters:VALID_RADII.includes(radius as typeof VALID_RADII[number])?(radius as typeof VALID_RADII[number]):DEFAULT_ATLAS_STATE.radiusMeters,windowDays:VALID_WINDOWS.includes(days as typeof VALID_WINDOWS[number])?(days as typeof VALID_WINDOWS[number]):DEFAULT_ATLAS_STATE.windowDays,activeGroups:groups&&groups.length>0?groups:[...DEFAULT_ATLAS_STATE.activeGroups]};}
export function serializeUrlState(state:AtlasState){const params=new URLSearchParams();params.set('lat',state.place.coordinates[1].toFixed(5));params.set('lng',state.place.coordinates[0].toFixed(5));params.set('place',state.place.label);if(state.place.detail)params.set('detail',state.place.detail);params.set('radius',String(state.radiusMeters));params.set('days',String(state.windowDays));params.set('groups',state.activeGroups.join(','));return`?${params.toString()}`;}
